---
import Layout from '../../layouts/Layout.astro';
import Navigation from '../../components/Navigation.astro';
import Card from '../../components/Card.astro';
import BackgroundCanvas from '../../components/BackgroundCanvas';
import Footer from '../../components/Footer.astro';
---

<Layout title="Products">
    <BackgroundCanvas client:only="react" />
    <div class="layout-container">
        <Navigation active="Catalog" />

        <div class="products-header">
            <h1>Our Plugin Catalog</h1>
            <p>High-performance WordPress plugins that actually respect your server.</p>
        </div>

        <Card
            title="Smooth Search"
            kicker="Available Now"
            description="The first WooCommerce search engine powered by WebAssembly. Blazing fast indexing and retrieval directly in the browser."
            variant="peach"
            span={8}
            class="product-card"
        >
            <div class="product-card-content">
                <div class="product-stats">
                    <div class="stat">
                        <span class="stat-value">200ms</span>
                        <span class="stat-label">Response Time</span>
                    </div>
                    <div class="stat">
                        <span class="stat-value">0</span>
                        <span class="stat-label">DB Queries</span>
                    </div>
                    <div class="stat">
                        <span class="stat-value">100K+</span>
                        <span class="stat-label">Products Supported</span>
                    </div>
                </div>
                <a href="/products/smooth-search" class="product-cta">
                    View Details
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                        <polyline points="12 5 19 12 12 19"></polyline>
                    </svg>
                </a>
            </div>
        </Card>

        <Card
            title="More Amazing Plugins"
            kicker="Coming Soon™"
            variant="lavender"
            span={4}
            class="coming-soon-card"
        >
            <div class="coming-soon-content">
                <div class="construction-emoji">🚧</div>
                <p class="coming-soon-text">Our Rust engineers are currently caffeinating themselves to bring you more performance wizardry.</p>
                <div class="progress-bar">
                    <div class="progress-fill"></div>
                </div>
                <p class="progress-label">Compiling awesomeness...</p>
            </div>
        </Card>

        <Card
            title="We're Just Getting Started"
            kicker="A Note from the Team"
            variant="white"
            span={12}
            class="message-card"
        >
            <div class="message-content">
                <p class="message-text">
                    Yes, we know. One plugin does not a "catalog" make. But hey, Rome wasn't built in a day, and neither is a Rust-powered plugin empire!
                    We're currently in our laboratory (read: frantically debugging) working on some seriously cool stuff that'll make your WordPress site go brrrr.
                </p>
                <p class="message-text">
                    In the meantime, why not give Smooth Search a spin? We promise it's faster than waiting for us to finish the next plugin.
                    Your patience and support mean the world to us — and if you want to speed things up, we accept coffee donations in the form of GitHub stars. ⭐
                </p>
                <div class="support-actions">
                    <a href="#" class="support-link">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                        </svg>
                        Star us on GitHub
                    </a>
                    <a href="#" class="support-link">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path>
                            <path d="M13.73 21a2 2 0 0 1-3.46 0"></path>
                        </svg>
                        Get notified of new releases
                    </a>
                </div>
            </div>
        </Card>

        <Footer />
    </div>
</Layout>

<style>
    .products-header {
        grid-column: 1 / -1;
        padding: 40px 0;
    }

    .products-header h1 {
        font-size: 64px;
        font-weight: 700;
        letter-spacing: -2px;
        line-height: 1;
        margin-bottom: 16px;
    }

    .products-header p {
        font-size: 20px;
        color: var(--c-text-secondary);
        margin-top: 8px;
    }

    .product-card-content {
        display: flex;
        flex-direction: column;
        gap: 32px;
        margin-top: 24px;
    }

    .product-stats {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
    }

    .stat {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .stat-value {
        font-size: 36px;
        font-weight: 700;
        letter-spacing: -1px;
        color: var(--c-text-primary);
    }

    .stat-label {
        font-size: 14px;
        color: var(--c-text-secondary);
        font-weight: 500;
    }

    .product-cta {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: var(--c-primary);
        color: var(--c-primary-inverse);
        padding: 16px 32px;
        border-radius: var(--radius-pill);
        font-weight: 600;
        font-size: 16px;
        transition: all 0.3s var(--ease-smooth);
        align-self: flex-start;
    }

    .product-cta:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 30px var(--c-shadow);
    }

    .product-cta svg {
        transition: transform 0.3s var(--ease-smooth);
    }

    .product-cta:hover svg {
        transform: translateX(4px);
    }

    .coming-soon-card {
        min-height: 300px;
    }

    .coming-soon-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        gap: 20px;
        padding: 20px 0;
    }

    .construction-emoji {
        font-size: 64px;
        animation: bounce 2s ease-in-out infinite;
    }

    @keyframes bounce {
        0%, 100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-10px);
        }
    }

    .coming-soon-text {
        font-size: 16px;
        line-height: 1.6;
        color: var(--c-text-secondary);
    }

    .progress-bar {
        width: 100%;
        height: 8px;
        background: var(--c-bg);
        border-radius: var(--radius-pill);
        overflow: hidden;
        margin-top: 8px;
    }

    .progress-fill {
        height: 100%;
        width: 42%;
        background: linear-gradient(90deg, var(--c-lavender), var(--c-peach));
        border-radius: var(--radius-pill);
        animation: shimmer 2s ease-in-out infinite;
    }

    @keyframes shimmer {
        0% {
            opacity: 0.6;
        }
        50% {
            opacity: 1;
        }
        100% {
            opacity: 0.6;
        }
    }

    .progress-label {
        font-size: 12px;
        font-family: monospace;
        color: var(--c-text-secondary);
        opacity: 0.7;
    }

    .message-content {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .message-text {
        font-size: 18px;
        line-height: 1.7;
        color: var(--c-text-secondary);
    }

    .support-actions {
        display: flex;
        gap: 16px;
        margin-top: 12px;
        flex-wrap: wrap;
    }

    .support-link {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        padding: 14px 24px;
        background: var(--c-bg);
        border-radius: var(--radius-pill);
        font-weight: 600;
        font-size: 15px;
        color: var(--c-text-primary);
        transition: all 0.3s var(--ease-smooth);
        border: 1px solid var(--c-border);
    }

    .support-link:hover {
        background: var(--c-primary);
        color: var(--c-primary-inverse);
        transform: translateY(-2px);
        box-shadow: 0 8px 20px var(--c-shadow);
    }

    @media (max-width: 1024px) {
        .products-header h1 {
            font-size: 48px;
        }

        .products-header p {
            font-size: 18px;
        }

        .product-stats {
            grid-template-columns: repeat(3, 1fr);
            gap: 16px;
        }

        .stat-value {
            font-size: 28px;
        }
    }

    @media (max-width: 640px) {
        .products-header h1 {
            font-size: 36px;
        }

        .product-stats {
            grid-template-columns: 1fr;
            gap: 20px;
        }

        .stat {
            text-align: center;
        }

        .product-cta {
            width: 100%;
            justify-content: center;
        }

        .support-actions {
            flex-direction: column;
        }

        .support-link {
            width: 100%;
            justify-content: center;
        }

        .message-text {
            font-size: 16px;
        }
    }
</style>
